components {
    comp(arg1,arg2) {
        div_componentu {
            "arg1 = " $arg1
            "arg2 = " $arg2
        }
    }

    c1() { c2() }
    c2() { c1() }
}

html {
<
$var=2+2
$var2=4
>
div {
}

each [1,2,3,4,5,6] as var {
"Row: " + $var
}
    switch(2+2) {
        4 -> div{
        "In here" + STRING(2)
        }
        else -> div{}
    }

    "Hello "+ STRING(2)

   STRING(2+2)
   STRING($var+$var2)

   div {
       <
           $arr = [1,2,3,4,5]
           $nietablica = 15
           $liczba = 15

           //$przypisanie_nieistniejacej_zmiennej = $nieistniejaca_zmienna
       >
           //"wyłuskanie nie intem " + $arr["xd"]

           //"wyłuskanie nietablicy " + $nietablica[3] //TODO FIX

           //"out of bounds " + $arr[5] //TODO FIX

           //"nielegalna_operacja" % 2 //TODO FIX

           "odejmowanie 5-3= " + STRING(5 - 3) //TODO zły wynik
           "mnozenie 5*3= " + STRING(5 * 3) //TODO zły wynik
           "wiekszy 11 > 2= " + STRING(11 > 2) //TODO zły wynik
           "mniejszy 11 < 2= " + STRING(11 < 2) //TODO zły wynik

           //switch(4) { //TODO FIX
           //    $liczba -> "jeden"
           //    else -> "niejeden"
           //}

           //switch($arr) { //TODO FIX
           //     1 -> "jeden"
           //     else -> "niejeden"
           //}

           //each [$liczba, $nieistniejaca] as var { //TODO error at 0,0
           // "liczba: " + $var
           //}

           //$arr //TODO FIX
   }

   //repeat("nieliczba") {
   //     "repeat(nieliczba)"
   //}

   comp("za","duzo", "argumentow") //TODO ignoruje zbedny argument, można naprawić ale niekoniecznie

   //comp("za_malo_argumentow") //TODO FIX

    //c1() //TODO co w przypadku stackoverflow
    //TODO error gdy podany plik nie istnieje
}